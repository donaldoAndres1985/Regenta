package com.regenta.ventas.domain;

/** El CHECK de {@code pagos_venta.metodo} lleva esta lista. */
public enum MetodoDePago {
    EFECTIVO,
    TARJETA_DEBITO,
    TARJETA_CREDITO,
    TRANSFERENCIA,
    QR,
    CREDITO,
    BONO,
    OTRO
}
