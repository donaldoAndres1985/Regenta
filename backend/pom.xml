<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.regenta</groupId>
  <artifactId>regenta-backend</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>Regenta backend</name>
  <description>Microservicios de Regenta. Un modulo por servicio; se despliegan por separado.</description>

  <!-- Las versiones se declaran UNA sola vez, aqui. Ningun modulo hijo
       repite una version: si lo hace, el test de estructura falla. -->
  <properties>
    <java.version>21</java.version>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <spring-boot.version>3.5.14</spring-boot.version>
    <spring-cloud.version>2025.0.3</spring-cloud.version>
    <maven-compiler-plugin.version>3.13.0</maven-compiler-plugin.version>
    <maven-surefire-plugin.version>3.2.5</maven-surefire-plugin.version>
    <maven-resources-plugin.version>3.3.1</maven-resources-plugin.version>
    <maven-jar-plugin.version>3.4.1</maven-jar-plugin.version>
    <maven-toolchains-plugin.version>3.3.0</maven-toolchains-plugin.version>
  </properties>

  <modules>
    <module>gateway</module>
    <module>servicio-usuarios</module>
    <module>servicio-clientes</module>
    <module>servicio-inventario</module>
    <module>servicio-ventas</module>
    <module>servicio-compras</module>
    <module>servicio-recursos</module>
    <module>servicio-reservas</module>
    <module>servicio-menu</module>
    <module>servicio-mesas</module>
    <module>servicio-comandas</module>
    <module>servicio-facturacion</module>
    <module>servicio-caja</module>
    <module>servicio-alertas</module>
    <module>servicio-reportes</module>
    <module>servicio-auditoria</module>
    <module>estructura</module>
  </modules>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>org.springframework.cloud</groupId>
        <artifactId>spring-cloud-dependencies</artifactId>
        <version>${spring-cloud.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <!-- Las versiones de los plugins tambien se fijan aqui. Sin esto, Maven
             usa las de su super-POM, que en versiones viejas trae el compilador
             3.1 y compila contra Java 5: el build falla con
             "Source option 5 is no longer supported" segun la maquina. -->
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${maven-compiler-plugin.version}</version>
          <configuration>
            <release>${java.version}</release>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${maven-surefire-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-resources-plugin</artifactId>
          <version>${maven-resources-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-jar-plugin</artifactId>
          <version>${maven-jar-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-toolchains-plugin</artifactId>
          <version>${maven-toolchains-plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-maven-plugin</artifactId>
          <version>${spring-boot.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>

    <plugins>
      <!-- Maven puede correr sobre otro JDK (17, por ejemplo); el proyecto se
           compila y se prueba con 21. El plugin descubre los JDK instalados en la
           maquina y elige uno que cumpla; si no encuentra ninguno, el build falla
           diciendolo, en vez de compilar contra la version equivocada.
             mvn toolchains:display-discovered-jdk-toolchains   -> que ve Maven
             mvn toolchains:generate-jdk-toolchains-xml         -> genera toolchains.xml -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-toolchains-plugin</artifactId>
        <executions>
          <execution>
            <id>seleccionar-jdk</id>
            <goals>
              <goal>select-jdk-toolchain</goal>
            </goals>
            <configuration>
              <version>[${java.version},)</version>
              <discoverToolchains>true</discoverToolchains>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
