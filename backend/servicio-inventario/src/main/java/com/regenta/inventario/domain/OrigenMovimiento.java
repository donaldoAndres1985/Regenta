package com.regenta.inventario.domain;

/** El CHECK de {@code movimientos_inventario.origen_tipo} lleva esta lista. */
public enum OrigenMovimiento {
    VENTA, COMPRA, RECEPCION, AJUSTE, TRASLADO, DEVOLUCION, COMANDA, RESERVA, CARGA_INICIAL, SAGA
}
