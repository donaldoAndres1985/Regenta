package com.regenta.inventario.domain;

/** El CHECK de {@code bodegas.tipo} lleva esta lista. */
public enum TipoBodega {
    PRINCIPAL,
    SECUNDARIA,
    TRANSITO,
    DEVOLUCIONES,
    AVERIAS
}
