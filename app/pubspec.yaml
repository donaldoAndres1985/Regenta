name: regenta_workspace
description: "Raiz del monorepo melos de Regenta. No es un paquete publicable ni ejecutable; solo agrupa."
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.10.0

dev_dependencies:
  flutter_lints: ^6.0.0
  melos: ^6.3.2
